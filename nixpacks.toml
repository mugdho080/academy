[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install", "cd server && npm install"]

[phases.build]
cmds = ["npm run build:frontend", "npm run build:server"]

[start]
cmd = "node server/dist/index.js"
